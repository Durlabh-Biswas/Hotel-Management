import random
import pymysql
DB= pymysql.connect(host='localhost',user='t1',password='x')
CR=DB.cursor()
CR.execute(f"CREATE DATABASE Hotel_Taj")
CR.execute(f"use Hotel_Taj")
CR.execute(f"create table Admin(ROOMNO int primary key, AVAILABILITY char(1), SUITE char(1), PID int(5))")
CR.execute(f"create table Waitlist")
CR.execute(f"use Hotel_Taj")

print('Admin Page')
def 
